<Window x:Class="Asset_Management_System.Views.PromptWithTextInput"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d">
    <StackPanel Style="{StaticResource PromptStackPanel}" Background="Beige">
        <TextBlock x:Name="Label" Text="LABEL TEXT HERE" Style="{StaticResource PromptTextBlock}"/>
        <TextBox x:Name="Input" Style="{StaticResource PromptTextBox}"/>
        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Name="BtnOk" Content="OK" Style="{StaticResource PromptButton}" Grid.Column="0" Click="BtnOk_Click" />
            <Button Name="BtnCancel" Content="Cancel" Style="{StaticResource PromptButton}" Grid.Column="1" Click="BtnCancel_Click" />
        </Grid>
    </StackPanel>
</Window>